#include "nemo_tests.h"

int * testCreateLPID(int neuronsPerCore, int totalLPs){

	tw_lpid *lpid_arr = malloc(totalLPs * sizeof tw_lpid);

	for(int gid = 0; gid < totalLPs; gid ++ ) {

		tw_lpid[gid] = tw_lpid()

	}
}